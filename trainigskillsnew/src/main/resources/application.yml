#Name of the application
spring:
  application:
    name: trainingSkillsnewService

#Eureka server url for registering
#This is eureka client
eureka:
  client:
    register-with-eureka: true
    fetch-registry: true
    serviceUrl:
      defaultZone: http://localhost:7090/eureka/
#register url to server    

instance:
  hostname: localhost

server:
  port: 8847